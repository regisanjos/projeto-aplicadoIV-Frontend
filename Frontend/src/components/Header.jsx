import React from "react";
import "./dashboard.css";

const Header = () => {
    return (
        <header className="header">
            <h1>Dashboard</h1>
        </header>
    );
};

export default Header;
